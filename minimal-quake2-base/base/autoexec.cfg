set vid_renderer gl3

unbindall

bind w +forward
bind s +back
bind a +moveleft
bind d +moveright

bind SPACE +moveup
bind SHIFT +speed

bind MOUSE1 +attack

bind ESCAPE togglemenu

bind F10 quit
bind F12 screenshot

bind ` toggleconsole
bind ~ toggleconsole

alias d1 "map test1"
